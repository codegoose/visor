#include "main.rc" // Include the resource file for the main application

#include "../../libs/version/version.h" // Include the version header file from the library

const std::string sc::version::app_name = VER_APP_NAME; // Define the application name using the version information
const std::string sc::version::app_ver = VER_APP_VER; // Define the application version using the version information
